import rospy 
from std_msgs.msg import Float32

def timeL_cb(data):
    global timeL
    timeL = data.data

def timeL_cb(data):
    global timeL
    timeL = data.data

def timeL_cb(data):
    global timeL
    timeL = data.data

def timeL_cb(data):
    global timeL
    timeL = data.data

